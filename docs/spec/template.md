# 仕様書: {feature-name}

> 目的: 「何を作るか」を合意するための実装仕様。

## 1. 概要

- `{この機能が解決すること}`

## 2. 対象ユーザー / 利用シーン

- `{誰が}`
- `{いつ使うか}`

## 3. 機能要件

### 3.1 `{use-case-1}`

- `{要件1}`
- `{要件2}`

### 3.2 `{use-case-2}`

- `{要件1}`
- `{要件2}`

## 4. 非機能要件

- `{パフォーマンス要件}`
- `{セキュリティ要件}`
- `{アクセシビリティ要件}`

## 5. データ構造

```ts
interface {ModelName} {
  id: string;
}
```

## 6. 画面フロー

```text
{screen-a} -> {action} -> {screen-b}
```

## 7. スコープ外

- `{今回この仕様で扱わない範囲}`

## 8. 関連

- ADR: `docs/adr/{adr-file}.md`
- Context: `docs/context/domain.md`
